import React, { FC } from 'react';
import { Button } from 'antd';
import './App.css';

const App: FC = () => (
  <div className="App">
    <Button type="primary" className="m-10">
      Button
    </Button>
  </div>
);

export default App;
